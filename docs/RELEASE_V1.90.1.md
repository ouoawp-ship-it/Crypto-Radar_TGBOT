# v1.90.1

## 修复

- 将 Binance USDⓈ-M 实时行情默认地址从已退役的未路由 `/ws` 切换到官方 `/market/ws`。
- 环境同步只在检测到旧 Binance 地址时自动迁移，保留用户自定义地址。
- 更新脚本在 `git pull` 后自动重新载入新脚本，再执行依赖、测试、服务安装和重启，避免新增部署步骤需要手工运行两次。

## 验收

- Binance 新地址必须收到订阅确认和真实 `aggTrade` 事件。
- 发布后等待至少两个完整分钟桶，三家已启用交易所均须在 `/public-api/health` 中为 `ready`。
